# StakeMyGoal - MVP Demo

This is an MVP mockup of StakeMyGoal for local preview / StackBlitz / Codespaces.
- Uses Next.js 13 for compatibility with browser-based editors.
- UI is demo-only; API routes are placeholders.

Run locally:
1. npm install
2. npm run dev
3. Open http://localhost:3000
